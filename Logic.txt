# 🧠 MudMind Alpha 0.1: Ментальная карта системы

## 🎯 ФИЛОСОФИЯ: ЧЕЛОВЕК + ИИ, НЕ БОТ

          ┌─────────────────────────────────────────┐
          │              ИГРОК (Капитан)            │
          │  • Стратегия, квесты, диалоги           │
          │  • Финальное решение "что делать"       │
          │  • Контроль над критическими решениями  │
          └───────────────┬─────────────────────────┘
                          │ КОМАНДА / ПОДТВЕРЖДЕНИЕ
          ┌───────────────▼─────────────────────────┐
          │               ИИ (Первый офицер)        │
          │  • Обработка рутины                     │
          │  • Мгновенная реакция на опасность      │
          │  • Предложения, но не авто-действия     │
          └─────────────────────────────────────────┘


**Ключевая метафора**: Капитан корабля и первый офицер. Капитан задаёт курс (стратегия), офицер управляет рутиной (тактика), но в шторм капитан берёт штурвал.

## 🏗️ АРХИТЕКТУРНАЯ КАРТА: 4 СЛОЯ, 8 МОДУЛЕЙ

            [MUD Сервер]
                 │
        TCP/Telnet (сырой текст)
                 ▼
    ┌─────────────────────────────┐
    │   СЛОЙ 1: ТРАНСПОРТ         │
    │   MudClient (async)         │
    │   • Чтение/запись сокета    │
    │   • Декодирование кодировок │
    │   • Очистка ANSI-кодов      │
    └─────────────┬───────────────┘
                  │ MudLineReceived (событие)
                  ▼
    ┌─────────────────────────────┐
    │  ШИНА СОБЫТИЙ (EventBus)    │
    │  Pub/Sub хаба               │
    │  ┌─────────────────────┐    │
    │  │ Подписчики:         │    │
    │  │ • Parser            │    │
    │  │ • GUI               │    │
    │  │ • StateManager      │    │
    │  │ • KnowledgeBase     │    │
    │  └─────────────────────┘    │
    └─────────────┬───────────────┘
                  │ Разветвление события
         ┌────────┴────────┐
         ▼                 ▼
    ┌─────────┐    ┌─────────────┐
    │ СЛОЙ 2: │    │ СЛОЙ 3:     │
    │ ЯДРО    │    │ ИНТЕЛЛЕКТ   │
    │ (Core)  │    │ (Intellect) │
    └─────────┘    └─────────────┘


## 🔄 ЖИЗНЕННЫЙ ЦИКЛ ОДНОГО "ТИКА" (100-500мс)

### ФАЗА 0: ИНИЦИАЛИЗАЦИЯ (один раз при старте)


Загрузка приложения → Конфиг → Модель → GUI
        │               │         │       │
        ├─10 секунд────►│         │       │
        │               ├─3 секунды──────►│
        │               │         │       │
        └───────────────┴─────────┴───────┘


**Что загружается:**
1. Конфигурация (флаги пользователя)
2. Микро-модель GGUF (~500МБ, Q4_K_M)
3. GUI Dear PyGui с двумя панелями
4. EventBus с подписчиками

### ФАЗА 1: ПОЛУЧЕНИЕ СЫРОГО ТЕКСТА ОТ MUD


MUD → "Вы в тёмной пещере. Выходы: север, восток. Лежит меч."
      │
      TCP-поток (байты)
      │
      MudClient.decode()  (UTF-8/CP866)
      │
      MudClient.clean_ansi()  (удаляем цвета)
      │
      Разделение на строки по \n
      │
      ┌─ "Вы в тёмной пещере."
      ├─ "Выходы: север, восток."
      └─ "Лежит меч."
          │
          Каждая строка → MudLineReceived(текст, время)


**Буферизация**: Храним последние 5 строк в кольцевом буфере для контекста.

### ФАЗА 2: ПАРСИНГ - ПРЕВРАЩЕНИЕ ТЕКСТА В СТРУКТУРЫ

**Parser срабатывает когда:**
- Строка содержит ключевые слова (`HP:`, `упал`, `встал`)
- Прошло >500мс с последнего глубокого парсинга
- Длинная строка (>20 символов) - вероятно описание

**Что парсим и как:**


СТАТУС ПЕРСОНАЖА:
  Здоровье:  "HP: 45/100" → {hp: 45, max: 100, percent: 45}
  Статусы:   "Вы отравлены" → status_effects: ["poisoned"]
  Позиция:   "Вы упали" → position: "prone"

ОКРУЖЕНИЕ:
  Комната:   Между "━━━━━━━━" или после "Вы вошли в"
  Предметы:  "Лежит меч" → room_items: ["меч"]
  Выходы:    "Выходы: север, восток" → exits: ["north", "east"]
  Мобы:      "[Гоблин]" → mobs: [{"name": "Гоблин", "state": "агрессивный?"}]

БОЙ:
  Атака нас: "Гоблин бьёт вас!" → combat: true, last_attack_time: now()
  Наша атака: "Вы атакуете гоблина" → подтверждение действия
  Урон:      "Вы получаете 15 урона" → hp_delta: -15


**Обновление StateManager:**

Немедленно (dangerous flags):
  - hp_current, hp_max, hp_percent
  - status_effects (добавить/удалить)
  - position_state (стоя/лежит/сидит)
  - in_combat (да/нет)

Отложенно (требует контекста):
  - current_room (требует подтверждения через LOOK)
  - room_items (требует сканирования комнаты)


### ФАЗА 3: ДИАГНОСТИКА - КАКОВА НАША СИТУАЦИЯ?

**Classifier.evaluate(state) → флаги:**


in_combat = (последняя_атака < 5сек) ИЛИ (текст_о_бое) ИЛИ (hp_упал + не_отдых)
low_health = hp_percent < 30
critical_health = hp_percent < 10
hungry = статус "голоден" ИЛИ последняя_еда > 1час
prone = position == "prone" ИЛИ текст содержит "упал"
dark = комната содержит "темно" ИЛИ "не видно"
trapped = нет_выходов ИЛИ все_выходы_закрыты


**RuleEngine.apply_rules(state, flags):**


// Пример правила из user_rules.json
{
  "id": "combat_prone",
  "condition": "in_combat AND prone AND low_health",
  "priority": "CRITICAL",
  "action_sequence": ["встать", "щит", "бежать"],
  "timeout_ticks": 3,
  "cancel_if": "!prone"  // Отменить если уже встали
}


**Приоритеты активированных правил:**

CRITICAL    (уровень 0-1) → красный, авто-действие при таймауте
HIGH        (уровень 2)   → оранжевый, предложить 3 варианта
MEDIUM      (уровень 3)   → жёлтый, предложить 3 варианта
LOW         (уровень 4)   → зелёный, предложить если пользователь не занят


### ФАЗА 4: ПОСТРОЕНИЕ ПРОМПТА - ЧТО СКАЗАТЬ ИИ?

**PromptBuilder собирает контекст:**


[СИСТЕМА]
Ты помощник в текстовой MUD. Сгенерируй ровно 3 команды.

[КОНТЕКСТ]
Последние сообщения:
1. "Вы в тёмной пещере."
2. "Гоблин атакует!"
3. "Вы получаете 15 урона. HP: 45/100"

[СОСТОЯНИЕ]
HP: 45%, Статусы: здоров, Позиция: стоим, Бой: да

[АКТИВНЫЕ ПРАВИЛА]
1. CRITICAL: В бою с низким HP
2. HIGH: В тёмном помещении

[ИСТОРИЯ УСПЕШНЫХ КОМАНД]
1. встать → успех (когда упал)
2. щит → успех (в бою)
3. бежать север → успех (при низком HP)

[ОГРАНИЧЕНИЯ]
• Только команды MUD (1-2 слова)
• Без объяснений, только команды
• MUD принимает 1 команду в тик
• Не предлагать неизвестные команды

[ОЖИДАЕМЫЙ ФОРМАТ]
1. команда_первая
2. команда_вторая
3. команда_третья


### ФАЗА 5: ГЕНЕРАЦИЯ - МИКРО-МОДЕЛЬ ДУМАЕТ


Микро-модель (Vikhr-Qwen-2.5-0.5b-Instruct.Q4_K_M)
   │
   ├── Контекст: 1024 токена (история + промпт)
   │
   ├── Параметры генерации:
   │    • temperature: 0.3 (мало креатива, много следования правилам)
   │    • max_tokens: 50 (короткий ответ)
   │    • stop: ["\n\n", "###", "[КОНЕЦ]"]
   │
   └── Ответ: "1. щит\n2. атаковать гоблина\n3. бежать север"
        │
        Пост-обработка:
        1. Извлекаем строки по шаблону /^\d+\.\s*(.+)$/m
        2. Очищаем: удаляем кавычки, лишние пробелы
        3. Валидируем: проверяем в списке разрешённых команд (50+)
        4. Результат: ["щит", "атаковать гоблина", "бежать север"]


### ФАЗА 6: УПРАВЛЕНИЕ ОЧЕРЕДЬЮ - КОГДА ЧТО ОТПРАВЛЯТЬ

**CommandSequencer работает как диспетчер:**


Текущая очередь: [
  {command: "встать", sent_at_tick: 100, timeout: 103, state: "PENDING"}
]

Новые предложения: ["щит", "атаковать", "бежать"], priority: "CRITICAL"

Логика:
1. Если priority == "CRITICAL" AND текущий_приоритет != "CRITICAL":
   • Попытаться отменить текущую команду (отправить "\n")
   • Очистить очередь

2. Если очередь пуста:
   • Отправить первую команду ("щит")
   • Записать в очередь с timeout = текущий_тик + N

3. Если очередь не пуста:
   • Проверить таймаут текущей команды
   • Если timeout пройден → пометить как "TIMEOUT"
   • Отправить следующую

Таймауты:
• Простые команды (look, get): 2 тика (~1-2 секунды)
• Боевые команды: 3 тика
• Сложные (встать, каст): 4 тика
• Критические: 2 тика (быстрее)


### ФАЗА 7: GUI - ПОКАЗАТЬ И ЖДАТЬ ВЫБОРА

**Правая панель (советник):**


┌─────────────────────────────┐
│ [РЕЖИМ: СОВЕТНИК]          │
│                             │
│ 🔴 КРИТИЧЕСКАЯ СИТУАЦИЯ    │  ← Цвет по приоритету
│ HP: 45%, В бою, Тёмно      │
│                             │
│ (•) 1. щит                 │  ← Радио-кнопки
│ ( ) 2. атаковать гоблина   │
│ ( ) 3. бежать север        │
│                             │
│ [ СВОЯ КОМАНДА: ______ ]   │  ← Поле ввода
│ [ ОТПРАВИТЬ ] [ ПРОПУСТИТЬ ]│
└─────────────────────────────┘


**Логика взаимодействия:**


Игрок видит 3 варианта →
  1. Кликает на радио-кнопку → команда отправляется немедленно
  2. Вводит свою команду → валидация → отправка
  3. Ничего не делает 5 секунд:
     • Если priority == CRITICAL → авто-выбор команды1
     • Иначе → очистить предложения

Левя панель (вывод MUD) с подсветкой:
  • Жирный: предметы ("лежит МЕЧ")
  • Курсив: подсказки ("кажется, здесь что-то есть...")
  • Красный: опасность ("Гоблин АТАКУЕТ!", "яд")
  • Зелёный: успех ("Вы подняли меч")


### ФАЗА 8: АДАПТАЦИЯ - УЧИМСЯ НА ОТВЕТАХ MUD

**Ответ MUD может быть:**


Успех:        "Вы поднялись на ноги" → prone = false
Неудача:      "Вы не можете это сделать сейчас" → логируем ошибку
Частичный:    "Вы пытаетесь встать..." → флаг "пытается_встать" = true
Неожиданный:  "Вы телепортировались" → in_combat = false, location_change = true


**Обучение на результате:**


Успешная команда:
  • Добавить в "успешные паттерны": контекст → команда
  • Увеличить вес этого выбора в подобных ситуациях
  • Пример: "prone + combat + low_hp" → "встать" (успех: +1)

Неудачная команда:
  • Добавить в "избегаемые паттерны"
  • Попробовать альтернативу в похожей ситуации
  • Пример: "лестница" → "climb" (неудача) → запомнить не пробовать в этом контексте

Сохранение в SQLite:
  Таблица player_patterns:
    • context_hash: хеш ситуации (флаги + окружение)
    • chosen_command: что выбрали
    • result: SUCCESS/FAILURE/TIMEOUT
    • timestamp: когда


## 🎮 ТРИ ОСНОВНЫХ СЦЕНАРИЯ РАБОТЫ

### СЦЕНАРИЙ 1: ЭКСПЛОРИНГ (transit_mode = False)


Обнаружение новой зоны:
  • Детект: ЗАГЛАВНЫЕ выходы, "средний уровень: 2", уникальное название
  • Действие: Отправить "verbose on" серверу
  • Цель: Полное исследование, безопасность

Парсинг детальный:
  ┌─────────────────────────────────────┐
  │ Выходы:                             │
  │   • север [открыт]                  │
  │   • восток [закрыт, требуется ключ] │
  │                                     │
  │ Предметы:                           │
  │   • меч (на полу) - безопасен       │
  │   • сундук (закрыт) - требует осмотра│
  │                                     │
  │ Мобы:                               │
  │   • гоблин [агрессивный, ур. 2]     │
  │                                     │
  │ Риски:                              │
  │   • "холодный ветерок" → возможна ловушка│
  │                                     │
  │ Подсказки:                          │
  │   • "странные символы на стене" → квест│
  └─────────────────────────────────────┘

Предложения ИИ:
  1. examine восток
  2. search углы
  3. examine символы

Особенности:
  • Не предлагать go пока не examined выходы
  • Риски имеют приоритет над исследованием
  • Квестовые подсказки только отмечать, не активировать
  • Completion % = (осмотренных выходов / всего * 50%) + (проверенных предметов/мобов * 50%)


### СЦЕНАРИЙ 2: ТРАНЗИТ (transit_mode = True)


Известная зона → скорость важнее деталей

Парсинг минимальный:
  • Выходы: какие открыты
  • Предметы: только ценные (монеты, ключи)
  • Мобы: только если агрессивные
  • Статус: голод/жажда

Предложения ИИ:
  1. go север  (следующий по пути)
  2. get монеты
  3. eat хлеб  (если auto_routine)

Рутина (адаптивная):
  Отслеживание инвентаря из логов:
    • "Вы купили хлеб" → добавить хлеб в инвентарь
    • "Вы съели хлеб" → удалить хлеб, обновить время последней еды
    • "Вы положили зелье в сумку" → знать что зелье в сумке

  Адаптивные команды:
    • Если голоден И есть еда в инвентаре → eat <еда>
    • Если еда в контейнере → get <еда> from <контейнер> → eat <еда>
    • Если после еды осталось → put <остаток> in <контейнер>


### СЦЕНАРИЙ 3: БОЙ (самый сложный)


Иерархия решений в бою:

           [Начало боя]
                │
         ┌──────┴──────┐
         ▼             ▼
    HP > 50%       HP < 50%
         │             │
    ┌────┴─────┐  ┌────┴─────┐
    ▼          ▼  ▼          ▼
 Атака    Пнуть   Защита  Бегство/Телепорт
           (если
          cooldown=0)

Прогноз исхода:
  DPS (урон в секунду) = средний_урон / время_между_атаками
  Тиков_до_смерти = HP_игрока / DPS_моба
  Шанс_победы = (DPS_игрока / (DPS_игрока + DPS_моба)) * 100%
  
  Пример: HP=45, моб_урон=10/тик → 4.5 тика до смерти → КРИТИЧЕСКО

Особенности FIFO:
  MUD обрабатывает 1 команду за раз, есть задержка.
  Нельзя: "встать" → сразу "атаковать" (нужно ждать ответа)
  Правильно: "встать" → ждать "Вы поднялись" → "атаковать"
  
  Очередь команд:
    Тик 1: отправили "встать", state=PENDING, timeout=тик+4
    Тик 2: ждём ответа, ничего не отправляем
    Тик 3: получили "Вы пытаетесь встать..." → state=EXECUTING
    Тик 4: получили "Вы поднялись" → state=COMPLETED, очередь очищена
    Тик 5: можно отправлять следующую команду

Критические состояния:
  HP < 10%: ТЕЛЕПОРТ любой ценой (отменить всё остальное)
  HP < 30% + prone: ВСТАТЬ → ЩИТ → БЕЖАТЬ
  Нет выходов + в бою: ПОСЛЕДНИЙ БОЙ (максимальный урон)


## 🧩 ВНУТРЕННИЕ СТРУКТУРЫ ДАННЫХ

### SessionState (в памяти)


character:
  hp: {current: 45, max: 100, percent: 45}
  status_effects: ["poisoned", "bleeding"]
  position: "standing"  # standing/prone/sitting
  hunger: 70  # 0-100%
  thirst: 60  # 0-100%
  
environment:
  current_room: "Тёмная пещера"
  room_hash: "a1b2c3d4"  # для сравнения
  exits: [
    {direction: "north", state: "open", examined: true},
    {direction: "east", state: "closed", examined: false}
  ]
  items: [
    {name: "меч", state: "on_floor", examined: false, safe: true},
    {name: "сундук", state: "closed", examined: false, safe: false}
  ]
  mobs: [
    {name: "Гоблин", state: "aggressive", hp_indicator: "ранен"}
  ]
  risks: ["холодный ветерок", "склизкий пол"]
  hints: ["странные символы"]
  
combat:
  in_combat: true
  combat_start_time: 1234567890
  last_attack_received: 1234567895
  current_target: "Гоблин"
  mobs_in_combat: ["Гоблин"]
  
flags:
  transit_mode: false
  auto_routine: true
  aggro_mode: false
  auto_pickup: true
  auto_combat: false
  
knowledge:
  inventory: ["меч", "хлеб", "зелье здоровья"]
  containers: {сумка: ["монеты", "ключ"]}
  visited_zones: {
    "Тёмная пещера": {visited: true, explored: false, completion: 30},
    "Лесная опушка": {visited: true, explored: true, completion: 95}
  }
  learned_patterns: [
    {context: "prone+combat", action: "встать", success_rate: 0.9},
    {context: "ladder+immovable", action: "приставить", success_rate: 1.0}
  ]


### EventBus события


MudLineReceived(text, timestamp, raw_bytes)
  │
  ├──► Parser.parse() → StateUpdated
  ├──► GUI.add_text() (левая панель)
  ├──► KnowledgeBase.extract_items()
  └──► ContextManager.append()

StateUpdated(old_state, new_state, changes)
  │
  ├──► Classifier.evaluate() → SituationClassified
  ├──► RuleEngine.apply_rules() → RulesActivated
  └──► GUI.update_status_panel()

SituationClassified(flags, priority)
  │
  └──► PromptBuilder.build() → PromptReady

PromptReady(prompt_text, context_hash)
  │
  └──► MicroModel.generate() → SuggestionsGenerated

SuggestionsGenerated(commands, priority, source)
  │
  ├──► GUI.show_suggestions() (правая панель)
  └──► CommandSequencer.receive()

UserCommandSelected(command, source)
  │
  └──► CommandSequencer.send() → CommandSent

CommandSent(command, timestamp, expected_timeout)
  │
  └──► MudClient.send_raw()

CommandCompleted(command, result, response_time)
  │
  ├──► KnowledgeBase.learn_pattern()
  ├──► ContextManager.record()
  └──► GUI.show_result()


## 🚨 СТРЕСС-ТЕСТ: ПОЛНЫЙ СЦЕНАРИЙ ОТ ТИКА К ТИКУ

### Исходное состояние
```
Тик 0: HP=100%, стоя, не в бою, transit_mode=False (эксплоринг)
```

### Обнаружение новой зоны

Тик 1:
MUD → "Вы вошли в ЗАБРОШЕННЫЙ ХРАМ. ВЫХОДЫ: СЕВЕР, ЮГ, ВОСТОК."
Действия:
  • Parser: new_room_detected=true, zone_name="ЗАБРОШЕННЫЙ ХРАМ"
  • Classifier: transit_mode=False (остаётся, заглавные = новая зона)
  • Sequencer: отправить "verbose on"
  • GUI: добавить в visited_zones с completion=0%


### Начало исследования

Тик 2-3:
MUD → "verbose mode включён. Лежит разбитая амфора."
Действия:
  • Prompt: "Новая зона, эксплоринг. Предложи осмотр."
  • MicroModel: ["examine север", "look", "examine амфора"]
  • GUI: показать 3 варианта (приоритет MEDIUM)


### Внезапная опасность

Тик 4:
MUD → "Из темноты выскакивает СКЕЛЕТ! Атакует! Вы получаете 25 урона. HP: 75/100"
Действия:
  • Parser: in_combat=true, hp=75, mobs=["СКЕЛЕТ"]
  • Classifier: priority=HIGH (бой, но HP нормальное)
  • RuleEngine: правило "combat_start" → ["атаковать", "щит", "осмотреть"]
  • Sequencer: отправить "щит" (первая команда)


### Эскалация боя

Тик 5-6:
MUD → "Скелет сильно бьёт! Вы падаете. HP: 40/100"
Действия:
  • Parser: prone=true, hp=40
  • Classifier: in_combat=true, low_health=true (40% < 70%), prone=true
  • RuleEngine: активируется "combat_prone_critical" → CRITICAL
  • Sequencer: (уже выполняется "щит") ждём завершения


### Критическая ситуация

Тик 7:
MUD → "Вы подняли щит. HP: 38/100"
Действия:
  • Parser: команда "щит" завершена
  • Classifier: всё ещё critical (prone+combat+low_hp)
  • Prompt: "КРИТИЧЕСКО! Лежим в бою! HP 38%"
  • MicroModel: ["встать", "щит", "кричать о помощи"]
  • Sequencer: отправить "встать" (CRITICAL, прерывает очередь если была)


### Ожидание и ухудшение

Тик 8-9:
MUD → "Вы пытаетесь встать..." (тик 8)
MUD → "Скелет бьёт лежачего! HP: 18/100" (тик 9)
Действия:
  • Parser: hp=18 (!), prone=true (всё ещё на земле)
  • Classifier: теперь critical_health=true (18% < 30%)
  • RuleEngine: "hp<30_in_combat" → перекрывает, но команда "встать" уже в процессе
  • State: установлен флаг "waiting_for_stand_response"


### Переломный момент

Тик 10:
MUD → "Вы поднялись на ноги. HP: 16/100"
Действия:
  • Parser: prone=false (!), hp=16
  • Classifier: critical_health=true, in_combat=true, prone=false
  • Prompt: "СМЕРТЕЛЬНАЯ ОПАСНОСТЬ! HP 16% в бою стоя!"
  • MicroModel: ["телепорт", "бежать север", "последнее зелье"]
  • Priority: CRITICAL (hp<10% почти)
  • Sequencer: отправить "телепорт" (самая безопасная команда)


### Разрешение

Тик 11:
MUD → "Вы телепортируетесь в безопасное место."
Действия:
  • Parser: in_combat=false, location_changed=true
  • Classifier: безопасное состояние
  • Prompt: "В безопасности. HP 16%. Лечение и осмотр."
  • MicroModel: ["пить зелье", "осмотреться", "отдыхать"]
  • Priority: MEDIUM


### Обучение на опыте

После сценария:
KnowledgeBase добавляет:
  • Паттерн: "prone+combat+hp<40" → "встать" → SUCCESS
  • Паттерн: "hp<20+combat+standing" → "телепорт" → SUCCESS
  • В visited_zones: "ЗАБРОШЕННЫЙ ХРАМ" completion=10% (только вошли)


## ⚙️ ТЕХНИЧЕСКИЕ ОГРАНИЧЕНИЯ И РЕШЕНИЯ

### Ограничение 1: MUD принимает 1 команду за раз

Решение: CommandSequencer с очередью и таймаутами
  • Не спамить командами
  • Ждать подтверждения
  • Учитывать задержки сети


### Ограничение 2: Микро-модель ограничена контекстом

Решение: ContextManager
  • Хранит только последние 1024 токена
  • Приоритет: последние сообщения > состояние > история
  • Сжимает старые сообщения в summary


### Ограничение 3: Игрок может быть медленным

Решение: Приоритетная система
  • CRITICAL: авто-действие через 5 секунд
  • HIGH: подсветка, но ждём
  • MEDIUM/LOW: ждём сколько нужно


### Ограничение 4: Разные MUD имеют разный синтаксис

Решение: Конфигурируемые парсеры
  • Регулярки в конфиге
  • Плагиновая система (в будущем)
  • Обучение на лету (запоминает форматы HP, выхода и т.д.)


## 🎯 ИДЕАЛЬНЫЙ ПОТОК РАБОТЫ


Игрок подключается к MUD
     │
     MudMind загружается (10 секунд)
     │
     Игрок видит знакомый MUD-интерфейс
     │
     MudMind тихо парсит, предлагает незаметные улучшения
     │
     В рутине: предлагает "go north", "get sword" одним кликом
     │
     При опасности: подсвечивает красным, предлагает чёткие варианты
     │
     В кризисе: принимает решение за игрока (если настроено)
     │
     После боя: анализирует что произошло, учится
     │
     Игрок фокусируется на:
        • Куда идти дальше (стратегия)
        • С кем говорить (диалоги)
        • Какие квесты брать
        • Что покупать/продавать
     │
     MudMind заботится о:
        • Не заблудиться
        • Не умереть от голода
        • Не пропустить ценный предмет
        • Не упустить момент для атаки
        • Встать когда упал
        • Сбежать когда почти мёртв


## 💡 САМАЯ ВАЖНАЯ МЫСЛЬ

**MudMind — это не бот, который играет за вас. Это первый офицер на вашем корабле.**

Вы капитан. Вы задаёте курс: "Идём в подземелье дракона". MudMind следит за тем, чтобы:
- Паруса были настроены правильно (рутина)
- Скалы замечены заранее (опасности)
- Команда готова к бою (подготовка)
- В шторм предлагает оптимальный манёвр (кризис)

Но штурвал всегда в ваших руках. Последнее слово — всегда за вами. Кроме ситуаций, где промедление — смерть. Тогда MudMind действует по вашим же правилам, которые вы ему задали.

Это баланс между контролем и автоматизацией, между тактикой и стратегией, между человеком и ИИ.